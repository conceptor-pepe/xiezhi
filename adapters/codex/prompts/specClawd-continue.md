# specClawd Continue

Continue the active specClawd workflow from `workflow-state.json`.

You must:

- read the current workflow state first
- advance only one stage at a time
- stop when user confirmation is required
- preserve all artifact updates for the current stage
