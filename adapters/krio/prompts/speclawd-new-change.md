# Speclawd New Change

Create a new Speclawd change from repository templates and stop before implementation.

Required outputs:

- change name
- created artifact list
- confirm `docs/changes/<date>-<name>/` contains `change.md`, `spec-delta.md`, `design.md`, `tasks.md`, `review.md`, `audit.md`, `test-review.md`, `commit-summary.md`, `archive.md`, and `error-memory.md`
- next step: `speclawd:spec-brief`
