# Speclawd Spec Brief

Prepare `spec-delta.md`, `design.md`, and `tasks.md` for the current change before implementation begins.

Use this order:

1. data structures
2. API contract
3. module coupling
4. task split

Must preserve:

- phase 0.5 `Step 1 -> Step 4`
- `REQ-*`, `DES-*`, `RISK-*`, `TASK-*`, and `CASE-*` traceability
- risk coverage mapping
- smallest shippable task slices
